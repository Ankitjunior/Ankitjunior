/* c program to calculate volume of a cuboid*/
#include<stdio.h>
#include<conio.h>
int main()
  {
  int l,b,h,v;
  printf("enter the length,breadth and height of cuboid in centimetr");


scanf("%d%d%d",&a,&b,&c);
  v=l*b*h;
  printf("volume of cubio will be %d",v);
  getch();
  
